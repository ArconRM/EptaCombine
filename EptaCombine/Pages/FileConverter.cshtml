@page
@model EptaCombine.Pages.FileConverterModel
@{
    ViewData["Title"] = "Конвертация файлов";
}
@Html.AntiForgeryToken()

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
<link rel="stylesheet" href="css/fileConverter.css"/>

<div class="container py-1">
    <h2 class="text-center mb-5 fw-bold">🛠 Конвертация файлов</h2>

    <!-- Загрузка файла -->
    <div id="upload-section" class="card shadow-sm mb-4 p-4 hover-zoom">
        <label for="uploadFile" class="form-label fw-semibold">1️⃣ Выберите файл:</label>
        <input type="file" class="form-control" id="uploadFile" name="uploadFile"/>
    </div>

    <!-- Выбор формата -->
    <div id="format-section" class="card shadow-sm mb-4 p-4 d-none hover-zoom">
        <p>📁 Категория файла: <strong id="categoryLabel"></strong></p>
        <label for="formatSelect" class="form-label fw-semibold">2️⃣ Выберите выходной формат:</label>
        <select id="formatSelect" class="form-select mb-3"></select>
        <button id="convertBtn" class="btn btn-outline-primary w-100 fw-bold transition-hover">🚀 Выполнить спецоперацию
        </button>
    </div>

    <!-- Прогресс -->
    <div id="progress-section" class="alert alert-info text-center d-none">
        ⏳ Обработка файла, пожалуйста, подождите (3 дня)...
    </div>

    <!-- Скачивание -->
    <div id="result-section" class="card text-center p-4 d-none fade-in border-success border-2">
        <p class="mb-3 text-success fw-semibold">✅ Операция завершена!</p>
        <a id="downloadLink" href="#" download class="btn btn-success w-50 mx-auto shadow">Скачать файл</a>
    </div>
</div>

@section Scripts {
    <script src="~/js/fileConverter.js?v=@DateTime.Now.Ticks"></script>
}